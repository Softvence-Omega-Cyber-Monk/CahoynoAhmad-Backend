model Plan {
    id String @id @default(uuid())
    name String
    price Float
    description String
    duration Int
    features String[]
    priceId String
    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt
}