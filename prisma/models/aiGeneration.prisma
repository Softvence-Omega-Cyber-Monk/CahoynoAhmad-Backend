model AiGeneration {
  id        String   @id @default(cuid())
  userId    String
  userPlan  String
  createdAt DateTime @default(now())
  tone      String?
  textLength Int?
  
  @@map("ai_generations")
  @@index([userId, createdAt])
}